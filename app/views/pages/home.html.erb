<h2>We're here to help you find Hours of Happiness</h2>

<ul>
  <% @places.each do |place| %>
    <li><%= link_to "#{place.name}", place_path(place) %>
  <% end %>
</ul>

<%= link_to "Add New Place", new_place_path %>
